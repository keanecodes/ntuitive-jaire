<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="sg.ntuitive.jaire">

    <uses-permission android:name="android.permission.BLUETOOTH" />
    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme.NoActionBar">
        <activity android:name="sg.ntuitive.jaire.main.ModelActivity" />
        <activity android:name="sg.ntuitive.jaire.main.WorkoutActivity"/>
        <activity android:name="sg.ntuitive.jaire.main.WorkoutPauseActivity"/>
        <activity android:name="sg.ntuitive.jaire.main.WorkoutSummaryActivity"/>
        <activity android:name="sg.ntuitive.jaire.main.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
<!--        <activity   android:name="com.shimmerresearch.android.guiUtilities.ShimmerBluetoothDialog"-->
<!--            android:theme="@android:style/Theme.Dialog"-->
<!--            android:label="@string/select_device">-->
<!--        </activity>-->

        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />
    </application>

</manifest>